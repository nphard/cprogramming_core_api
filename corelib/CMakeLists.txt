PROJECT(CORELIB C)
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

ADD_SUBDIRECTORY(src lib)

INSTALL(FILES COPYRIGHT README DESTINATION doc)
INSTALL(DIRECTORY doc/ DESTINATION doc)
INSTALL(PROGRAMS testlib.sh DESTINATION bin)

